# 🔧 حل مشكلة Three.js Import Error

## 📌 المشكلة
ظهور خطأ عند استيراد Three.js:
```
Cannot find module './three-D0xR9ADs.js'
```

## ✅ الحلول المقترحة

### الحل 1: تنظيف Cache وإعادة البناء (الأسرع)

افتح Terminal واكتب الأوامر التالية:

```bash
# 1. أوقف الخادم (Ctrl + C)
# 2. حذف node_modules و cache
rm -rf node_modules
rm -rf .vite
rm -rf dist

# 3. إعادة تثبيت الحزم
npm install

# 4. إعادة تشغيل الخادم
npm run dev
```

### الحل 2: التأكد من تثبيت Three.js

```bash
npm install three@^0.182.0
```

### الحل 3: مسح Cache المتصفح

1. افتح DevTools (`F12` أو `Ctrl+Shift+I`)
2. اذهب إلى **Application** tab
3. في القائمة الجانبية:
   - **Storage** → **Clear site data**
4. أعد تحميل الصفحة (`Ctrl+Shift+R`)

### الحل 4: تعطيل Service Worker مؤقتاً (للتطوير)

في ملف `/vite.config.ts`، عطّل Service Worker:

```typescript
devOptions: {
  enabled: false, // غيّر من true إلى false
  type: 'module',
},
```

ثم أعد تشغيل:
```bash
npm run dev
```

---

## 🧪 التحقق من نجاح الحل

بعد تطبيق أحد الحلول، تأكد من:

✅ عدم ظهور أخطاء في Console  
✅ تحميل المكتبات بشكل صحيح  
✅ عمل مصمم المطبخ 3D بدون مشاكل  

---

## 📝 ملاحظات مهمة

- **Three.js موجود بالفعل** في `package.json` (النسخة `^0.182.0`)
- المشكلة غالباً من **cache** أو **Service Worker** القديم
- التكوين الحالي صحيح 100%، لا تغيّر `vite.config.ts` إلا إذا لزم الأمر

---

## 🆘 إذا استمرت المشكلة

جرّب الحل الكامل:

```bash
# 1. حذف كل شيء
rm -rf node_modules dist .vite

# 2. حذف package-lock
rm -f package-lock.json

# 3. إعادة التثبيت
npm install

# 4. إعادة البناء
npm run build

# 5. معاينة النسخة المبنية
npm run preview
```

---

## 🌐 مشكلة GitHub Pages

إذا كان التطبيق يعمل محلياً (`npm run dev`) لكن لا يعمل على GitHub Pages:

1. تأكد من push آخر تعديلات:
```bash
git add .
git commit -m "Fix Three.js import issue"
git push origin main
```

2. أعد deploy:
```bash
npm run deploy
```

3. انتظر 2-3 دقائق ثم افتح الرابط:
   - https://[username].github.io/Graphitube/

---

## ✅ الحل النهائي (مجرب)

الحل الأسهل والأسرع:

```bash
# في Terminal:
rm -rf .vite dist
npm run dev
```

هذا يحذف cache ويعيد تشغيل الخادم بدون مشاكل!

---

**📞 للمساعدة:**  
إذا استمرت المشكلة، تواصل معي عبر WhatsApp: 0609394003
