# 🚀 دليل النشر السريع - Graphitube PWA

## ✅ المشروع جاهز بالكامل!

تطبيق Graphitube PWA مُهيأ بالكامل للنشر على GitHub Pages في المسار `/Graphitube/`.

---

## 📋 التحقق من الإعدادات

تم تكوين كل شيء بشكل صحيح:

- ✅ **`vite.config.ts`**: `base: '/Graphitube/'` ✓
- ✅ **`App.tsx`**: `<BrowserRouter basename={import.meta.env.BASE_URL}>` ✓
- ✅ **PWA Plugin**: مُفعّل ومُهيأ للعمل بدون إنترنت 100% ✓
- ✅ **Service Worker**: تسجيل تلقائي مع استراتيجيات التخزين المؤقت ✓
- ✅ **GitHub Actions**: ملف `.github/workflows/deploy.yml` جاهز ✓
- ✅ **الأيقونات**: أيقونات PWA في مجلد `public/` ✓
- ✅ **صفحة Offline**: صفحة احتياطية متعددة اللغات ✓

---

## 🎯 خطوات النشر

### الطريقة 1: GitHub Actions (موصى بها - تلقائية)

#### الخطوة 1: رفع المشروع إلى GitHub

```bash
# تهيئة Git (إن لم يكن مُهيأً)
git init

# إضافة كل الملفات
git add .

# حفظ التغييرات
git commit -m "Initial commit - Graphitube PWA ready for deployment"

# إضافة الريبو البعيد (استبدل YOUR_USERNAME باسم المستخدم الخاص بك)
git remote add origin https://github.com/YOUR_USERNAME/Graphitube.git

# رفع إلى فرع main
git branch -M main
git push -u origin main
```

#### الخطوة 2: تفعيل GitHub Pages

1. اذهب إلى الريبو على GitHub
2. اضغط **Settings** ← **Pages**
3. تحت **Source**، اختر: **GitHub Actions**
4. احفظ

#### الخطوة 3: النشر!

GitHub Actions سيقوم **تلقائياً** بـ:
- ✅ تثبيت الحزم
- ✅ بناء المشروع (`npm run build`)
- ✅ النشر على GitHub Pages

الموقع سيكون متاحاً على: `https://YOUR_USERNAME.github.io/Graphitube/`

---

### الطريقة 2: النشر اليدوي (باستخدام gh-pages)

```bash
# البناء والنشر في أمر واحد
npm run deploy
```

هذا سيقوم بـ:
- بناء المشروع → `dist/`
- رفع `dist/` إلى فرع `gh-pages`
- النشر تلقائياً

**ملاحظة:** بعد أول نشر، اذهب إلى **Settings → Pages** واختر:
- Source: **Deploy from a branch**
- Branch: **gh-pages** / **root**

---

## 🧪 اختبار PWA

### اختبار محلي

```bash
# البناء
npm run build

# المعاينة (مع Service Worker)
npm run preview
```

افتح: `http://localhost:4173/Graphitube/`

### التحقق من Service Worker

**DevTools → Application → Service Workers:**
- ✅ Status: "activated"
- ✅ Scope: يجب أن يتضمن `/Graphitube/`

**DevTools → Application → Cache Storage:**
- ✅ `workbox-precache-v2-...`
- ✅ `graphitube-pages`
- ✅ `graphitube-images`
- ✅ `graphitube-api`
- ✅ `graphitube-fonts`

### اختبار وضع Offline

1. افتح الموقع
2. DevTools → **Network**
3. ضع علامة على **Offline**
4. أعد تحميل الصفحة
5. ✅ يجب أن يعمل! (يعرض offline.html عند الحاجة)

---

## 📱 تثبيت PWA

### على الحاسوب (Chrome/Edge)

1. زر: `https://YOUR_USERNAME.github.io/Graphitube/`
2. ابحث عن زر **Install** في شريط العنوان
3. اضغط → **Install**
4. التطبيق يفتح كنافذة مستقلة

### على الهاتف

**Android:**
1. افتح في Chrome
2. القائمة → **إضافة إلى الشاشة الرئيسية**
3. تأكيد التثبيت

**iOS:**
1. افتح في Safari
2. زر المشاركة → **إضافة إلى الشاشة الرئيسية**
3. اكتب اسم التطبيق → **إضافة**

---

## 🔧 حل المشاكل الشائعة

### المشكلة: الروابط تعطي خطأ 404

**الحل:** تم التعامل معها!
- `public/404.html` يعيد التوجيه إلى `index.html`
- React Router يتعامل مع التوجيه من جانب العميل

### المشكلة: الملفات لا تُحمّل

**التحقق:**
```bash
# كل مسارات الملفات يجب أن تتضمن /Graphitube/
/Graphitube/assets/index-abc123.js ✅
/assets/index-abc123.js ❌
```

**الإصلاح:** تأكد من `base: '/Graphitube/'` في `vite.config.ts`

### المشكلة: Service Worker لا يتحدث

**الحل:**
```bash
# مسح الكاش
rm -rf dist node_modules/.vite

# إعادة البناء
npm install
npm run build
```

**في المتصفح:**
1. DevTools → Application → Service Workers
2. ضع علامة على **Update on reload**
3. اضغط **Unregister**
4. Hard refresh (Ctrl+Shift+R)

---

## 📊 مخرجات البناء

بعد `npm run build`:

```
dist/
├── index.html
├── manifest.webmanifest        # بيان PWA
├── sw.js                        # Service Worker
├── workbox-*.js                 # Workbox runtime
├── assets/
│   ├── index-[hash].js         # الحزمة الرئيسية
│   ├── index-[hash].css        # الأنماط
│   ├── vendor-[hash].js        # المكتبات
│   └── three-[hash].js         # Three.js
├── icon-192.png
├── icon-512.png
└── offline.html
```

---

## 🎯 الميزات المُفعّلة

### تقسيم الكود (Code Splitting)

✅ **vendor chunk**: React, React Router, مكتبات UI
✅ **three chunk**: Three.js (منفصل لتحسين الأداء)
✅ التحميل الكسول للمسارات

### استراتيجيات التخزين المؤقت

✅ **الصفحات**: NetworkFirst (محتوى جديد مع احتياطي)
✅ **API**: NetworkFirst مع تخزين 5 دقائق
✅ **الصور**: CacheFirst (30 يوم)
✅ **الخطوط**: CacheFirst (سنة)
✅ **الملفات الثابتة**: مخزنة مسبقاً للوصول الفوري

### الدعم دون اتصال

✅ كل الملفات الثابتة تُخزن في أول زيارة
✅ صفحة احتياطية (`/offline.html`) بثلاث لغات
✅ قائمة انتظار للطلبات المُرسلة بدون إنترنت

---

## 🚀 الخطوات التالية

### 1. النشر على Google Play Store

استخدم **Bubblewrap CLI**:

```bash
# التثبيت
npm install -g @bubblewrap/cli

# التهيئة
bubblewrap init --manifest https://YOUR_USERNAME.github.io/Graphitube/manifest.webmanifest

# بناء APK
bubblewrap build

# تثبيت على الجهاز
bubblewrap install
```

راجع: `PWA_ANDROID_README.md` للدليل الكامل

### 2. مراقبة الأداء

استخدم **Lighthouse** في Chrome DevTools:
- درجة الأداء (Performance)
- درجة PWA (يجب أن تكون 100)
- إمكانية الوصول (Accessibility)
- أفضل الممارسات
- SEO

### 3. إضافة التحليلات (اختياري)

```bash
npm install react-ga4
```

تتبع:
- عدد الزيارات
- إرسال النماذج
- استخدام مصمم 3D
- التفاعلات دون اتصال

---

## 🎉 مبروك!

تطبيق **Graphitube PWA** الخاص بك الآن:

✅ يعمل بدون إنترنت بنسبة 100%
✅ قابل للتثبيت على جميع الأجهزة
✅ منشور على GitHub Pages
✅ جاهز لـ Google Play Store

**الرابط المباشر:** `https://YOUR_USERNAME.github.io/Graphitube/`

---

## 📚 المراجع

**المشاكل الشائعة:**
- [TROUBLESHOOTING.md](./TROUBLESHOOTING.md)
- [QUICK_FIX.md](./QUICK_FIX.md)

**التوثيق:**
- [Vite PWA Plugin](https://vite-pwa-org.netlify.app/)
- [Workbox](https://developer.chrome.com/docs/workbox/)
- [GitHub Pages](https://docs.github.com/en/pages)

---

**صُنع بـ ❤️ لـ Graphitube**
