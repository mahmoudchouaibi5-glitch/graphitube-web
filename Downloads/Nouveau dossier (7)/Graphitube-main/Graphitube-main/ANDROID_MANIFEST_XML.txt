<?xml version="1.0" encoding="utf-8"?>
<!-- 📱 Graphitube Android App - AndroidManifest.xml -->
<!-- المسار: app/src/main/AndroidManifest.xml -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- إذن الإنترنت - ضروري لـ TWA -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Graphitube"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:targetApi="31">

        <!-- 🌐 TWA Launcher Activity -->
        <!-- هذا النشاط يفتح الموقع في وضع ملء الشاشة -->
        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:exported="true"
            android:label="Graphitube">

            <!-- Intent filter للتشغيل من launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 🔗 Digital Asset Links -->
            <!-- للتحقق من ملكية النطاق -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE"/>
                
                <!-- النطاق المسموح -->
                <data 
                    android:scheme="https"
                    android:host="graphitube.netlify.app"/>
            </intent-filter>

            <!-- ⚙️ TWA Configuration -->
            
            <!-- URL الافتراضي للتطبيق -->
            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="https://graphitube.netlify.app" />
                
            <!-- لون شريط الحالة -->
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@android:color/white" />
                
            <!-- لون شريط التنقل -->
            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@android:color/white" />
                
            <!-- وضع العرض -->
            <meta-data
                android:name="android.support.customtabs.trusted.DISPLAY_MODE"
                android:value="standalone" />
                
            <!-- اتجاه الشاشة -->
            <meta-data
                android:name="android.support.customtabs.trusted.SCREEN_ORIENTATION"
                android:value="portrait" />

        </activity>

    </application>

</manifest>
